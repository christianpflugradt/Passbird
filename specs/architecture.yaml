schema_version: 1
last_reviewed: 2026-05-30
architecture:
  style: onion
  enforcement:
    file: src/test/kotlin/de/pflugradts/passbird/PassbirdTest.kt
    mechanisms:
      - ArchUnit onion architecture rules
      - repository-wide production package import coverage
      - package placement assertions
      - naming constraints
      - event-handler constraints
      - command outcome state constraints
      - boot graph registration completeness checks
layers:
  - name: domain_model
    package: de.pflugradts.passbird.domain.model
    responsibilities:
      - represent Eggs, Nests, Proteins, Slots, Shells, and domain events
      - keep domain state transitions explicit
      - avoid infrastructure concerns
    must_not_depend_on:
      - application layer
      - adapter layer
  - name: domain_service
    package: de.pflugradts.passbird.domain.service
    responsibilities:
      - coordinate repositories and domain objects
      - own business operations on passwords and nests
      - publish and react to domain events
    may_access:
      - domain model
      - repositories in domain service package
  - name: application
    package: de.pflugradts.passbird.application
    responsibilities:
      - bootstrapping
      - command parsing and dispatch
      - configuration loading
      - process orchestration
      - factories that bridge runtime concerns into ports
  - name: adapters
    package: de.pflugradts.passbird.adapter
    responsibilities:
      - file IO
      - keystore access
      - clipboard access
      - terminal UI
      - temporary global hotkey registration
      - import and export serialization
structural_rules:
  - repositories must only be accessed from domain services
  - classes implementing AdapterPort interfaces must live under adapter packages
  - no concrete classes may live directly in de.pflugradts.passbird.adapter or de.pflugradts.passbird.domain
  - no class names may end with Impl
  - no class names may end with Helper
  - command handlers must use the TypedCommandHandler contract and declare their handled command class explicitly
  - domain event handlers must expose handled domain event classes through EventHandler.eventTypes and dispatch through EventHandler.handle
  - repository-wide production import coverage must include every package under de.pflugradts that is included in the jar
  - do not introduce java.util.Optional outside the explicit Optional.toOption bridge in de.pflugradts.kotlinextensions.OptionKt; the codebase uses custom Option and TryResult types
  - dependency wiring must stay in explicit Kotlin composition roots without Guice, jakarta.inject, or another runtime DI framework
  - no production or test code may depend on com.google.inject or jakarta.inject
  - runtime context such as home directory and initial slot must flow through RunContext values, not global singleton state
  - command execution outcome state must flow through session graph collaborators, not process-global or ThreadLocal state
  - SystemOperation is limited to allowlisted file-persistence primitives, clock access, and process exit; clipboard, terminal input, and keystore construction must stay in their adapter packages and are guarded by architecture tests
runtime_design:
  boot_sequence:
    - Main.kt validates the home directory argument and constructs the RunContext
    - LauncherGraph constructs the launcher boot path with the RunContext
    - launcher, migration, or setup boot path is selected through explicit graph factories that carry the same RunContext
    - pre-launch migration detectors may route startup into a dedicated migration boot path before the main application may continue
    - migration boot may construct compatibility adapters that can read legacy persistence formats without widening the normal runtime path
    - shared password-tree format helpers live under application.passwordtree so migration and backup orchestration can use them without depending on adapter internals
    - shared keystore format helpers live under application.keystore so launcher detection and migration-only adapters can classify passbird.sec without guessing from release history
    - password-tree restore and sync move eggs, favorites, memory, and nest shells as a single snapshot rather than repopulating nests through a service callback
    - PasswordTreeSyncService mediates repository sync for domain services that must not depend on EggRepository directly
    - password-tree mutations are staged against live aggregates but sync failure restores a deep last-synced snapshot before the command reports failure
    - ApplicationGraph wires adapters, services, and handlers explicitly
    - PassbirdApplication enters the interactive command loop
  interaction_model:
    - user input is parsed into command types
    - CommandFactory creates command objects
    - CommandBus dispatches to handlers
    - command dispatch propagates handler exceptions back to CommandInputHandler so failed handlers cannot complete as successful commands
    - CommandExecutionTracker tracks command outcomes as an application collaborator shared by the interactive session graph
    - handlers call application and domain services
    - event registries propagate side effects after persistence succeeds for mutating password and nest operations
    - domain-event dispatch propagates handler exceptions back to the originating processEvents caller and keeps queued domain events available when posting fails
    - inactivity monitoring may request shutdown from a daemon timer, but PassbirdApplication serializes the resulting QuitCommand on the interactive execution path instead of letting the timer thread post directly to the command bus
ports_and_adapters:
  - port: UserInterfaceAdapterPort
    implementation: adapter.userinterface.CommandLineInterfaceService
    concern: terminal input and output including secure input mode
  - port: ClipboardAdapterPort
    implementation: adapter.clipboard.ClipboardService
    concern: clipboard copy and reset behavior
  - port: GlobalHotkeyAdapterPort
    implementation: adapter.userinterface.hotkey.GlobalHotkeyService
    concern: temporary registration of a global NEXT hotkey for guided credential flows without leaking OS-specific APIs into application handlers; a shared application-layer backend policy owns accepted backends, runtime resolution, startup preparation, and macOS app-loop requirements, and the adapter uses that policy before instantiating any platform-specific registrar
  - port: KeyStoreAdapterPort
    implementation: adapter.keystore.KeyStoreService
    concern: load and store secret material in PKCS12, while migration boot may construct compatibility adapters for legacy JCEKS reads; JVM keystore construction stays inside adapter.keystore
  - port: PasswordTreeAdapterPort
    implementation: adapter.passwordtree.PasswordTreeFacade
    concern: encrypted persistence of eggs, favorites, memory, and nests as one snapshot
  - port: ExchangeAdapterPort
    implementation: adapter.exchange.FilePasswordExchange
    concern: import and export of human-readable password data
important_extension_paths:
  - change: add a new top-level command
    steps:
      - update command resolution in application.commandhandling.CommandType
      - add or extend a command class under application.commandhandling.command
      - wire a handler under application.commandhandling.handler
      - register the handler in ApplicationGraph commandHandlers
      - add focused tests for parsing and handler behavior
      - update README and specs/domain.yaml if user-facing behavior changes
  - change: add a new configuration option
    steps:
      - extend ReadableConfiguration and concrete configuration classes
      - ensure ConfigurationFactory can still load old files safely
      - define default behavior conservatively
      - document the option in CONFIGURATION.md, README if user-facing, and specs/security.yaml if it affects security
  - change: add a new adapter
    steps:
      - keep the port in the application or domain layer
      - implement the adapter under src/main/kotlin/de/pflugradts/passbird/adapter
      - construct it from the relevant boot graph
      - cover it with tests at the right level
  - change: modify password tree persistence
    steps:
      - treat as security-sensitive and compatibility-sensitive
      - inspect PasswordTreeReader, PasswordTreeWriter, and application.passwordtree together
      - verify signatures, checksum handling, memory layout, nest layout, and egg layout end to end
      - preserve or add explicit migration detection before the main boot path when the on-disk format changes
      - update migration docs if user data compatibility changes
high_risk_files:
  - src/main/kotlin/de/pflugradts/passbird/application/boot/main/ApplicationGraph.kt
  - src/main/kotlin/de/pflugradts/passbird/application/commandhandling
  - src/main/kotlin/de/pflugradts/passbird/application/passwordtree
  - src/main/kotlin/de/pflugradts/passbird/adapter/passwordtree
  - src/main/kotlin/de/pflugradts/passbird/application/security
